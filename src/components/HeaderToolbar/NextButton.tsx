import { ArrowFront } from '@assets/icons/ArrowFront';
import * as S from './NextButton.style';

const NextButton = () => {
  return (
    <S.NextButtonWrapper className="evd-toolbar-next-button">
      <ArrowFront />
    </S.NextButtonWrapper>
  );
};

export default NextButton;

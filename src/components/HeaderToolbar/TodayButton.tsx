import * as S from './TodayButton.style';

const TodayButton = () => {
  return <S.TodayButtonWrapper className="evd-toolbar-today-button">Today</S.TodayButtonWrapper>;
};

export default TodayButton;
